<svg width="180" height="50" viewBox="0 0 180 50" fill="none" xmlns="http://www.w3.org/2000/svg">
  <!-- Subtitle -->
  <text x="0" y="12" font-family="IBM Plex Sans, sans-serif" font-size="9" font-weight="500" fill="#475569" letter-spacing="0.5" text-transform="uppercase">
    ТЕНДЕРНОЕ СОПРОВОЖДЕНИЕ
  </text>

  <!-- ICTD text logo -->
  <text x="0" y="35" font-family="DM Sans, sans-serif" font-size="28" font-weight="900" fill="#0052A3" letter-spacing="-0.5">
    ICTD
  </text>

  <!-- .ru domain -->
  <text x="90" y="35" font-family="DM Sans, sans-serif" font-size="28" font-weight="500" fill="#475569">
    .ru
  </text>

  <!-- Accent mark -->
  <rect x="0" y="42" width="50" height="3" fill="#F59E0B" rx="1.5"/>
</svg>
